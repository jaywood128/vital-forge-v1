---
# For more options, see https://docs.honeybadger.io/lib/ruby/gem-reference/configuration

api_key: '<%= ENV["HONEYBADGER_API_KEY"] %>'

# The environment your app is running in.
env: "<%= ENV['HONEYBADGER_ENV'] || ENV['RAILS_ENV'] || 'staging' %>"

# The absolute path to your project folder.
root: "<%= Rails.root.to_s %>"

# Report errors even in staging (override default behavior)
report_data: true

# Honeybadger won't report errors in these environments.
development_environments:
- test
- development
- cucumber

# The current Git revision of your project. Defaults to the last commit hash.
# revision: null

# Enable verbose debug logging (useful for troubleshooting).
debug: false

# Enable Honeybadger Insights (only when API key is set, e.g. staging/production)
insights:
  enabled: <%= ENV["HONEYBADGER_API_KEY"].present? %>

# Sidekiq error reporting
sidekiq:
  attempt_threshold: 0  # Report all failed job attempts
