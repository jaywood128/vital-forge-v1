meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/login
  body: json
  auth: inherit
}

headers {
  X-CSRF-Token: {{csrfToken}}
}

body:json {
  {
    "user": {
      "email": "jaywood128@gmail.com",
      "password": "{{password}}"
    }
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
