meta {
  name: workouts
  type: http
  seq: 1
}

get {
  url: https://{{baseUrl}}/workouts
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
